# Session Log — 2026-06-18 MVP Implementation

**agent:** claude  
**branch:** feat/task-local-cultural-books-mvp  
**session:** continued from context-summary (prior session ran out of context)

---

## 本次完成內容

### Phase 1（上一 session 完成）
- spec/plan 補充 export template mapping 設計（commit 3844bc3）

### Phase 2（本 session 完成）

**commit 5497d27 — feat: implement import mapping workflow**
- `migrations/002_import_export_mapping.sql`：
  - ALTER TABLE vendor_books: 加 extra_fields TEXT, source_row_number INTEGER
  - ALTER TABLE import_profiles: 加 project_type, source_type, header_row, mappings, extra_field_settings
  - CREATE TABLE export_templates（含 column_mappings JSON）
  - ALTER TABLE export_jobs: 加 export_template_id
- `config.yaml`：加 export_templates 段（local_culture_kaohsiung_115，column_mappings A-J）
- `app/config.py`：新增 export_templates list, default_project_export_template_name 屬性
- `app/database.py`：ensure_initial_data() 加 export_templates seeding（INSERT OR IGNORE by name）
- `app/services/import_service.py`：
  - `_detect_header_row()`：從 _read_excel_with_detected_header 獨立出來
  - `preview_excel()`：無 DB 寫入，回傳 sheets/header/source_columns/suggested_mappings
  - `confirm_import()`：用使用者確認的 mappings 正式匯入，儲存 extra_fields/source_row_number
  - `import_vendor_books()`：更新 INSERT 包含 extra_fields=NULL, source_row_number=NULL（向下相容）
- `app/services/export_service.py`：
  - 移除 TEMPLATE_CONFIG 常數
  - ExportSettings 去除 template_path
  - `_load_export_template_for_project()` 從 DB export_templates 讀取範本設定
  - 以 column_index_from_string(col_map[key]) 取代硬編碼欄號
- `app/routers/imports.py`：加 POST /vendor-books/preview 和 /vendor-books/confirm 端點
- `app/routers/exports.py`：移除 template_path from ExportRequest，改讀 DB

**commit eab72da — feat: add import wizard UI**
- `app/static/import.html`：重寫書商書單標籤為 5-step wizard（A上傳→B工作表→C欄位對應→D額外欄位→E確認）
- `app/static/match.html`（使用者已修改）：filter 只顯示 available/already_owned；stats 顯示 ignored-ISBN counts
- `app/static/selection.html`：加 extra_fields 展開列（書商資訊），toggleExtra() 函式

**commit 6a8ba87 — fix: stabilize local startup dependencies**
- `requirements.txt`（使用者已修改）：pin bcrypt>=3.2.0,<4.0.0
- `start.bat`（使用者已修改）：優先 Astral CPython 3.12.13 / py -3.12；擋掉 3.14 RC
- `app/services/match_service.py`（使用者已修改）：isbn_status='valid' 過濾
- `app/routers/books.py`（使用者已修改）：stats 含 ignored_missing_isbn / ignored_invalid_isbn

---

## 驗證
- `python -m compileall app/ -q`：通過，無錯誤

## 尚待手動測試（下次 session 或使用者操作）
1. 啟動：`start.bat` → uvicorn 啟動
2. migration 002 自動執行（首次啟動時）
3. export_templates seeding（確認 config.yaml 設定載入正確）
4. 書商書單匯入 wizard：上傳 xlsx → B 步驟確認 header → C 欄位對應 → D 額外欄位 → E 確認匯入
5. 比對與選書，確認 extra_fields 顯示
6. 匯出：確認讀 export_templates DB，生成正確 Excel

## 已知限制
- import.html wizard 目前不支援套用已儲存的 profile（V2 功能）
- general_books 流程保留設計但未實作（符合任務範圍）
- export_templates 目前以 project_type 第一筆為準（V2 可支援多範本選擇）
