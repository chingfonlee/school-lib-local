---
date: 2026-06-22
ended_at: 2026-06-22T15:41:55
task: task-export-check-edit
agent: claude
---

## 做了什麼

任務完成。

- `validation_service.py`：`check_export_readiness()` details 補 `sel_id` 欄位
- `selection_service.py`：新增 `remove_selection()` 單筆刪除函式
- `selections.py`：新增 `DELETE /api/selections/{selection_id}` endpoint
- `export-check.html`：新增「操作」欄（移除 + 修正按鈕）、inline 修正表單、`bookDataMap`、`projectType`、`H_ALLOWED` 常數；一般圖書採購顯示 A/H/L 欄位；修正後自動刷新 `runCheck()`

## 驗證到哪

- `python -m compileall app`：pass
- 手動驗證：使用者確認移除與修正功能正常

## 未解決的問題

無

## 下次繼續

已完成
