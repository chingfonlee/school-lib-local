---
date: 2026-06-23
ended_at: 2026-06-23T16:16:06
task: task-project-delete-backup-restore
agent: claude
---

## 做了什麼

任務完成。

### 後端（app/routers/projects.py）

- 新增 `GET /api/projects/{project_id}/delete-preview`：回傳刪除影響統計（selection_count、export_job_count、import_batch_count、vendor_book_count、holding_count）
- 新增 `DELETE /api/projects/{project_id}`：在單一 transaction 中依正確順序刪除所有關聯資料，包含：
  - 先 DELETE selection_items、export_jobs
  - 再刪 book_matches（by vendor_book_id）、vendor_books
  - 先 UPDATE book_matches SET holding_id = NULL（處理跨專案 holding 參照），再刪 library_holdings
  - 最後刪 import_batches、procurement_projects
  - 發生錯誤時 rollback

### 後端（app/routers/backup.py）（新增）

- `GET /api/backup/database`：使用 sqlite3 backup API 在線備份，透過 FileResponse 下載，BackgroundTask 清理暫時檔
- `POST /api/backup/restore`：驗證 SQLite magic bytes + 必要資料表（procurement_projects / users / schema_migrations），備份現有 DB 至 restore_safety_backup，再還原，不需重啟服務

### app/main.py

- 註冊 backup router

### 前端（app/static/projects.html）

- 頁面頂部加入「資料庫管理」card：備份資料庫（`<a>` 直接下載）、還原資料庫（modal）
- 每個專案卡片加入 `.btn-danger-outline` 刪除按鈕
- 刪除確認 modal：呼叫 delete-preview 取得統計，`getProjectId()` 防止刪除 current project，`delete-confirm-btn` 直接 onclick binding
- 還原 modal：file input、`fetch()` 上傳 FormData、還原成功後 3 秒自動 reload
- 刪除後若被刪的是 current project，呼叫 `clearProject()`

### CSS（app/static/css/style.css）

- 新增 `.btn-danger-outline`（紅框低調樣式），不修改既有 `.btn-danger`

### 測試（新增）

- `tests/conftest.py`：in-memory SQLite fixture（`check_same_thread=False`）、`auth_client`、`backup_client`
- `tests/test_project_delete.py`：13 個新增測試，覆蓋 delete-preview、DELETE、跨專案 holding nullify、backup、restore 驗證

## 驗證到哪

- pytest：58 passed（含 13 個新增，既有 45 個全通過）
- `GET /api/projects/{id}/delete-preview`：不存在 → 404；存在 → 6 統計欄位 pass
- `DELETE /api/projects/{id}`：不存在 → 404；存在 → 200 + 列表確認移除 pass
- `GET /api/backup/database`：200、Content-Disposition 含日期、SQLite magic bytes 正確 pass
- `POST /api/backup/restore` 非 SQLite → 400；合法備份 → 200 + safety_backup 建立 + 還原後資料正常 pass
- projects.html 結構：刪除按鈕、modal、備份/還原 UI 全部到位 pass

## 未解決的問題

無

## 下次繼續

已完成
