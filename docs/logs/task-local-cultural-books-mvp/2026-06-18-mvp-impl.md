---
date: 2026-06-18
task: task-local-cultural-books-mvp
agent: claude
---

## 做了什麼

### Step 1：修正 spec/plan award_item 規則
- spec：將「獲獎項目與政策議題分類規則」改寫為「V1 local_culture 直接保留原文」
- spec：completeness_status needs_review 移除 award_notes 強制判斷條款
- spec：vendor_books 欄位說明改為 policy_topic/award_notes 為 V2 reserved
- spec：驗收條件修正（移除 SDGs/SEL 自動分離驗收，改為直接保留原文）
- plan：Step 4.4 改寫為 V1 直接保留原文，V2 reserved 說明
- plan：Step 6 completeness service 移除 award_notes 強制 needs_review 規則
- commit: `feat(task-local-cultural-books-mvp): clarify local culture award rules`

### Step 2：讀取教育局範本結構
- 使用 openpyxl 讀取範本儲存格，確認：
  - 標題列：A1 (merged A1:J1)
  - 校名：A3 (merged A3:D3)，格式「校名：○○國小」
  - 核定金額：E3 (merged E3:G3)
  - 欄位標題：第 4 列（A-J: 排序/書名/作者/出版社/ISBN/採購數量/定價/小計/獲獎項目/備註）
  - 範例列：第 5 列
  - 資料列：第 6-55 列（預填序號 1-50）
  - L 欄 6-13：下拉選單選項（獲獎項目備選值）
  - 合計列：A56
  - 備註：A57，簽名：A58

### Step 3：完整 MVP 實作
建立所有目錄與檔案：
- `requirements.txt`、`config.yaml`、`start.bat`、`.gitignore`
- `migrations/001_initial_schema.sql`（8 張資料表）
- `app/config.py`、`app/database.py`（migration runner + ensure_initial_data）
- `app/auth.py`（require_auth 依賴注入）
- `app/services/isbn_service.py`（normalize_isbn + get_isbn_status，含 float-as-string 修正）
- `app/services/completeness_service.py`（compute + recompute_for_book）
- `app/services/import_service.py`（library holdings + vendor books，column auto-detect）
- `app/services/match_service.py`（run_match，ISBN 索引比對 + 書名比對）
- `app/services/selection_service.py`（upsert/summary/list/clear）
- `app/services/validation_service.py`（check_export_readiness）
- `app/services/export_service.py`（TEMPLATE_CONFIG + export_local_culture，openpyxl）
- `app/routers/`（auth/projects/imports/books/selections/exports 共 6 個 router）
- `app/main.py`（FastAPI + SessionMiddleware + startup hook）
- `app/static/`（login/index/projects/import/match/selection/export-check/export 共 8 頁）
- `app/static/css/style.css`（Apple 風格 UI）
- `app/static/js/common.js`（共用工具函式）

### 驗收到哪
- `python -m compileall app/` 無錯誤
- isbn_service 單元測試：10 個案例全部通過（含 float-as-string 修正）
- completeness_service 單元測試：7 個案例全部通過
- feat branch push 至 GitHub: https://github.com/chingfonlee/school-lib-local/tree/feat/task-local-cultural-books-mvp

## 未解決的問題

1. **尚未安裝 dependencies**：需要在本地 Windows 環境執行 `start.bat` 才能安裝並啟動
2. **端對端流程尚未手動驗收**：需要實際匯入 `00_source/學校館藏.xls` 與書商書單，確認欄位對應
3. **教育局範本合計列**：A56 合計列只填入 F56（總冊數）與 H56（總金額），其他欄位格式（合計列底色/粗體）由範本原有格式保留
4. **selection.html 預算試算**：refreshBudget() 是獨立呼叫，數量改變後已觸發 API 更新；但若使用者在 overrides 修改了 list_price / purchase_price 後，預算試算仍顯示原始值，需重新載入頁面

## 下次繼續

1. 安裝 dependencies：`pip install -r requirements.txt`
2. 執行 `start.bat`（或 `python -m uvicorn app.main:app --host 127.0.0.1 --port 8765`）
3. 依 plan Step 18 執行端對端驗收
4. 若館藏欄位自動對應失準，在前端調整欄位對應並儲存 profile
