---
date: 2026-06-24
ended_at: 2026-06-24T15:01:21
task: task-project-card-status-actions
agent: claude
---

## 做了什麼

確認 Step 1–5 均已在本 session 或先前 commit 完成：

- **Step 1**（`app/routers/projects.py`）：`GET /api/projects/` 已擴充 `last_import`、`vendor_book_count`、`already_owned_count`、`selection_amount` 四個 subquery 欄位。
- **Step 2**（`app/static/projects.html`）：專案卡片已改為縱向結構（`project-card-top` / `project-card-status` / `project-card-nav`），顯示書商書目、選書、預算、匯出 4 行狀態摘要。
- **Step 3**（`app/static/projects.html`）：`goToStep(event, url, id, name)` helper 已新增；卡片底部快速按鈕（匯入、選書、匯出前檢查、匯出 Excel）均已接入；卡片主體及「選擇」按鈕導向 `/selection.html`。
- **Step 4**（`app/static/css/style.css`）：`.project-card` 改為 `flex-direction: column`；新增 `.project-card-top`、`.project-info`、`.project-card-status`、`.project-card-nav`；`::before` guide bar 不受影響。
- **Step 5**（`tests/test_project_list.py`）：新增 4 個 API 測試覆蓋新欄位預設值、vendor/holdings 批次過濾、兩種 `subtotal_mode` 計算；本 session 補一個 `test_root_redirects_to_projects` 驗證根路由 redirect。
- **Step 6**（`app/main.py`、`app/static/login.html`）：已於上一 session 完成，根網址 `GET /` → 302 `/projects.html`，login.html 登入成功導向 `/projects.html`。

## 驗證到哪

- pytest 63 passed，3 warnings（既有 deprecation）
- HTTP 驗證：`GET /` → 302 `/projects.html`；`GET /index.html` → 200；`GET /api/health` → 200
- 靜態確認：`login.html` 兩處 `window.location.href` 均為 `/projects.html`；`projects.html` 含 `goToStep` 及四個快速按鈕

## 未解決的問題

無

## 下次繼續

所有 plan 步驟（Step 1–6）均已完成並通過驗證。可進行 task-close 流程（merge 至 main）。
