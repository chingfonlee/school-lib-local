---
date: 2026-06-20
ended_at: 2026-06-20T22:44:49
task: task-library-holdings-reimport-multisheet
agent: claude
---

## 做了什麼

任務完成（含後補的自動比對同步）。

**原始範圍（多 sheet 覆蓋匯入）：**
- 改寫 `import_library_holdings()`：全量快照覆蓋（先清舊館藏再 INSERT）、多 sheet 逐一匯入、零筆 rollback 保護
- 新增 `_list_excel_sheets()`、`_read_library_sheet()`、`_insert_library_holding_rows()` helpers
- `app/static/import.html` 顯示覆蓋提示、各 sheet 筆數、略過 sheet 名稱

**補強範圍（匯入後自動比對同步）：**
- `import_library_holdings()`：commit 成功後查詢所有有 vendor_books 的 project，逐一呼叫 `run_match(pid)`，回傳 `match_rerun / affected_projects / match_stats_by_project`；失敗時回傳 `match_rerun_error`，匯入本身不失敗
- `confirm_import()` / `import_vendor_books()`：commit 成功後對本次 project 呼叫 `run_match(project_id)`，回傳 `match_stats`
- `import.html` 館藏匯入成功後顯示「已重新執行比對（N 個書單專案）」；書商書單匯入成功後顯示可採購 / 已館藏數量；有失敗時顯示警告

**Rebase 處理：**
- fix 分支落後 main 11 commits（含 task-selection-record-snapshot、task-clear-imported-source-data、task-cleared-source-selection-display），rebase 時解決 `_clear_library_holdings` 命名衝突（保留 main 的版本，移除重複定義）

## 驗證到哪

使用者測試確認：
- 學校館藏.xls 4 sheets 全部匯入，總計 15,869 筆
- 重複匯入採覆蓋快照，不累加
- 匯入後自動 run_match，match.html 首次開啟即顯示可採購 / 已館藏數量
- `python -m compileall app` pass
- `python -c "import app.services.import_service"` pass

## 未解決的問題

無

## 下次繼續

已完成
