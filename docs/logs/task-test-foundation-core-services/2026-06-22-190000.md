---
date: 2026-06-22
ended_at: 2026-06-22T19:00:00
task: task-test-foundation-core-services
agent: claude
---

## 做了什麼

任務完成。建立核心服務自動化測試基礎：

- `requirements.txt`：新增 `pytest>=8.0`
- `app/routers/imports.py`：抽取 `_resolve_project_type(conn, project_id) -> str` helper（封裝既有 L72–75 查詢與 fallback，INSERT 行為不變）
- `tests/__init__.py`：空檔
- `tests/test_completeness_service.py`：17 個測試，覆蓋 `compute()` 的 local_culture 分支（7 個）、general_books 分支（6 個）、overrides 影響（4 個）
- `tests/test_import_profile_project_type.py`：3 個測試，以 in-memory SQLite 驗證 `_resolve_project_type()` 的 general_books、local_culture、查無 project_id 三個情境

## 驗證到哪

- `python -m compileall app`：pass
- `python -m pytest -v`：20 passed, 0 failures

## 未解決的問題

無

## 下次繼續

已完成
