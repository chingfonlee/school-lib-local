---
date: 2026-07-01
ended_at: 2026-07-01T11:10:11
task: task-selection-add-page-owned-export
agent: claude
type: close
---

## Task Close — task-selection-add-page-owned-export

### 驗收結論

**PASS** — 後端 API、validation、export 邏輯全部通過驗證。

### 驗證摘要

- `pytest tests/ -v`：103 passed, 0 failed, 1 warning（含本次新增 19 tests）
- **API/Excel verification PASS**
  - bulk add available books → added/skipped 計數正確，已選書不重複加入
  - `force_owned=true` 的 already_owned 書目：`user_overrides={"force_owned": true}` 寫入 DB；export-check 回傳 `can_export=True`；ISBN 出現在匯出 Excel
  - 未確認 `force_owned` 的 already_owned 書目：export-check 回傳 `can_export=False`；ISBN 不出現在匯出 Excel
  - Cleanup：7 筆測試 selection records 已全部刪除

### 保留注意事項

- 前端 UI（按鈕位置、dialog 文案、toast 計數）無法在 headless 環境驗收，需使用者開啟瀏覽器人工最後確認。
- `/api/exports/check` detail 物件不含 `user_overrides` 欄位，但不影響 `can_export` 邏輯正確性。

### 變更範圍

- `app/static/selection.html`：PAGE_SIZE 100 → 50；加入本頁按鈕與 `addCurrentPage()` 函式
- `app/services/selection_service.py`：`upsert_selection` 支援 `force_owned` 參數；新增 `bulk_add_selections`
- `app/routers/selections.py`：新增 `POST /api/selections/bulk` endpoint
- `app/services/validation_service.py`：`_is_force_owned` helper；`can_export` 允許 `force_owned` already_owned
- `app/services/export_service.py`：`_is_force_owned` helper；Python 端過濾取代 SQL WHERE status 條件
- `tests/test_selection_add_page.py`（5 tests）
- `tests/test_validation_service_owned.py`（4 tests）
- `tests/test_export_service_owned.py`（10 tests）

### 後續

無遺留事項。可依需要人工瀏覽器確認前端 UI。
