---
date: 2026-07-01
ended_at: 2026-07-01T10:53:23
task: task-selection-add-page-owned-export
agent: claude
---

## 做了什麼

完成 task-selection-add-page-owned-export 的全部實作（一次 session 完成）：

- `app/static/selection.html`：PAGE_SIZE 由 100 改為 50；新增「加入本頁」按鈕與 `addCurrentPage()` 函式；本頁含未選 already_owned 時顯示確認 dialog；取消整批中止；確認後對 already_owned 書目設 `force_owned: true` 送出批次 API。
- `app/services/selection_service.py`：`upsert_selection` 加 `force_owned: bool = False` 參數，`force_owned=True` 時 INSERT 時將 `{"force_owned": true}` 寫入 `user_overrides`；新增 `bulk_add_selections`，先查既有 selection 再逐筆加入，已選書略過計入 skipped。
- `app/routers/selections.py`：新增 `POST /api/selections/bulk` endpoint，Pydantic 模型 `BulkSelectionItem`、`BulkSelectionRequest`。
- `app/services/validation_service.py`：新增 `_is_force_owned(overrides)` helper；`can_export` 條件改為 `already_owned` + `force_owned=true` 也允許匯出。
- `app/services/export_service.py`：新增 `_is_force_owned(book)` helper；兩個 export 函式改為 Python 端過濾（移除 SQL WHERE 中的 status 過濾，改在 fetchall 後以 list comprehension 過濾），不依賴 SQLite JSON1。
- 新增測試：`test_selection_add_page.py`（5 tests）、`test_validation_service_owned.py`（4 tests）、`test_export_service_owned.py`（10 tests）。

## 驗證到哪

`pytest tests/ -v`：103 passed, 0 failed, 1 warning（現有 103 tests 全部通過，含本次新增 19 tests）。

前端手測尚未執行（需要實際執行 server 與瀏覽器操作）。

## 未解決的問題

- 前端手測尚未完成：需實際跑 server，確認「加入本頁」按鈕位置、已館藏確認 dialog 文案、toast 計數、預算列刷新、卡片狀態更新。
- `tmp/` 目錄含有 portable-build site-packages，`pytest -v`（不限 tests/ 目錄）會觸發 224 個 collection error（pandas、passlib 的測試檔）；此為既有問題，非本 task 引入。建議日後在 `pyproject.toml` 或 `pytest.ini` 加 `testpaths = tests` 排除。

## 下次繼續

若使用者認為後端實作與測試已足夠，可進行手測驗收，或直接進入 Task Close 流程（merge 回 main）。
