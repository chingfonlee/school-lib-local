---
date: 2026-06-23
ended_at: 2026-06-23T06:17:37
task: task-project-status-dashboard
agent: claude
---

## 做了什麼

在 `index.html` 統計卡片下方加入「下一步建議」卡片（`#next-step-card`）：

- 新增 `renderNextStep(stats, selCount, readiness)` function，依序判斷專案狀態並顯示對應導引
- 判斷邏輯：館藏 = 0 → 匯入館藏；書商書單 = 0 → 匯入書單；可採購 = 0 → 查看比對結果；已選 = 0 → 前往選書；已選 > 0 → 顯示匯出準備度摘要 + 前往匯出前檢查
- 已選書 > 0 時，條件呼叫 `/api/exports/check`（帶 `price_field` + `subtotal_mode`），顯示就緒 / 待補充 / 缺必填各幾本
- 呼叫失敗時靜默降級，不影響頁面其他內容

## 驗證到哪

- `python -m compileall app`：pass
- `python -m pytest -v`：45 passed
- 使用者確認任務完成

## 未解決的問題

無

## 下次繼續

已完成
