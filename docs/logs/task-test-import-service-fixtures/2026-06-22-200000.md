---
date: 2026-06-22
ended_at: 2026-06-22T20:00:00
task: task-test-import-service-fixtures
agent: claude
---

## 做了什麼

任務完成。新增 import_service 測試並修正附帶發現的 bug：

**Bug 修正（Option A）：**
- `app/services/import_service.py`：`confirm_import()` 與 `import_vendor_books()` 的 `book` dict 各補入 `eligibility_label`、`recommendation_source`。修正前 general_books 匯入後 completeness 永遠落入 `missing_required`，修正後正確使用 general_books 規則。

**新增測試：**
- `tests/test_import_service_helpers.py`（Tier 1）：`_match_columns()` 13 個 case（eligibility_label、recommendation_source、award_notes、policy_topic、summary 的欄名對應、大小寫正規化、無對應情境）；`_is_blank_or_total_row()` 7 個 case
- `tests/test_confirm_import.py`（Tier 2）：以 in-memory SQLite + `_NoCloseConn` wrapper + openpyxl 動態 xlsx，驗證 general_books 欄位寫入（5 欄）、completeness 三個狀態（export_ready / missing_required / needs_review）、空白列與合計列跳過

## 驗證到哪

- `python -m compileall app`：pass
- `python -m pytest -v`：45 passed, 0 failures（含前一 task 的 20 個既有測試）

## 未解決的問題

無

## 下次繼續

已完成
