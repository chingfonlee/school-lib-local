---
date: 2026-06-22
ended_at: 2026-06-22T18:13:19
task: task-import-flow-guidance
agent: claude
---

## 做了什麼

改善 `import.html` 匯入流程的使用者導引：

1. 館藏匯入成功後，單一工作表亦顯示 sheet 名稱與筆數摘要（移除 `> 1` 限制，改為 `>= 1`）
2. 館藏匯入成功後，加入「前往比對結果 →」下一步連結
3. 書商書單匯入成功後，在「前往比對結果 →」旁加入「前往選書 →」連結
4. 新增 `isFriendlyChineseMessage()` 與 `formatImportError()` helper function
5. 四處 catch 套用 `formatImportError()`：館藏失敗、書商書單步驟 A / B / E 失敗，均以白話中文提示取代 Python 技術錯誤訊息

## 驗證到哪

- `python -m compileall app`：pass
- `python -m pytest -v`：45 passed（既有測試全部通過）
- 使用者確認任務完成

## 未解決的問題

無

## 下次繼續

已完成
