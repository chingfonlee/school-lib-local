---
date: 2026-06-19
ended_at: 2026-06-19T23:59:53
task: task-reimport-replace-vendor-books
agent: claude
---

## 做了什麼

任務完成。

**新增 `_clear_vendor_books_for_project(conn, project_id)` helper（`app/services/import_service.py`）**

- 在新批次插入前，清除同 project 所有 `batch_type='vendor_books'` 的舊資料
- 刪除順序：`selection_items` → `book_matches` → `vendor_books` → `import_batches`
- 使用 subquery `DELETE ... WHERE ... IN (SELECT ...)` 在 SQL 層完成，不自行 commit，由呼叫方 transaction 統一管控

**修改 `confirm_import` 與 `import_vendor_books`**

- 兩條匯入路徑均在 `conn = get_connection()` 後、INSERT `import_batches` 前，插入一行 `_clear_vendor_books_for_project(conn, project_id)`

**修正 Excel ROUND half-up 行為**

- `_resolve_formula_purchase_price` 原使用 Python `round()`（banker's rounding），262.5 → 262，與 Excel 結果不符
- 改用 `math.floor(x + 0.5)`（half-up），262.5 → 263，符合 Excel `ROUND(,0)` 行為

## 驗證到哪

- `python -m compileall app` → Pass
- ROUND test cases：G=700→525 ✓、G=350→263 ✓、G=250→188 ✓
- 重新匯入 `00_source/本土書單.xlsx` 後：
  - vendor_books 只剩最新批次 670 筆 ✓
  - purchase_price IS NOT NULL = 670 / 670 ✓
  - 第一筆 525、第二筆 263（修正前為 262）✓
  - selection_items 清空 ✓
  - library_holdings 不變（20003 筆）✓
  - 舊批次（id 3/5/7/8/9）已全數刪除 ✓

## 未解決的問題

無

## 下次繼續

已完成
