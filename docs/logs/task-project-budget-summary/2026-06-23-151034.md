---
date: 2026-06-23
ended_at: 2026-06-23T15:10:34
task: task-project-budget-summary
agent: claude
---

## 做了什麼

### 後端（app/routers/projects.py）

- `ProjectCreate` 與 `ProjectUpdate` 加入 Pydantic `field_validator`：`budget_amount` 不可為負數（422）
- `update_project` 改用 `body.model_fields_set` 判斷是否更新 `budget_amount`，支援 PUT 傳入 `null` 清空預算（修正原本 `if body.budget_amount is not None` 的 bug）

### 前端（app/static/projects.html）

- 新增專案表單加入「專案預算（元）」欄位（非必填，min=0）
- `createProject()` 加入預算讀取、驗證（負數阻止）及送出 `budget_amount`
- 專案卡片新增預算顯示行：`NT$ X,XXX 元` 或灰色「未設定」
- 編輯 Modal 加入預算欄位，`openEdit()` 預填 budget，`saveEdit()` 送出 `budget_amount`（含 null 清空）

### 前端（app/static/export-check.html）

- 頂層新增 `let proj = null`，初始化時賦值
- 加入「金額摘要」`.card`（`#budget-summary-card`）：顯示專案預算、定價總額、採購單價總額、應付金額（依 subtotal_mode）
- `renderBudgetSummary(items)` 函式：解析 `user_overrides`（安全 try/catch）、千分位格式化、超支紅色 / 剩餘綠色、預算未設定時不顯示差額
- `runCheck()` 結束後呼叫 `renderBudgetSummary`，切換 price-field / subtotal-mode 時自動更新

### 不需異動

- 無新 migration（`budget_amount REAL` 已在 migration 001）
- `export_service.py` 不變
- `validation_service.py` 不變
- Step 4（export.html 自動填入）：可選，未實作

## 驗證到哪

API 驗證（curl / Invoke-WebRequest）：

| 項目 | 結果 |
|------|------|
| POST budget_amount=45000 | ✓ 200 |
| POST budget_amount 留空 | ✓ 200 |
| POST budget_amount=-1 | ✓ 422 拒絕 |
| PUT budget 更新 | ✓ GET 確認 |
| PUT budget_amount=null 清空 | ✓ GET 確認 null |
| PUT budget_amount=-500 | ✓ 422 拒絕 |

HTML 結構驗證：

- projects.html：表單欄位、卡片模板、編輯 Modal、openEdit 參數 ✓
- export-check.html：13/13 結構檢查 ✓、price-field 聯動 3/3 ✓

pytest：45 passed ✓

## 未解決的問題

- Step 4（export.html 自動填入核定金額）可列為後續優化；目前使用者在 export.html 手動填入「核定金額」仍可正常運作

## 下次繼續

已完成
