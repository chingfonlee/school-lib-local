---
date: 2026-06-29
ended_at: 2026-06-29T23:07:11
task: task-expand-jh-project-type-checks
agent: claude
---

## 做了什麼

任務完成。

- 擴充 `migrations/001_initial_schema.sql` 與 `migrations/002_import_export_mapping.sql` 的 `project_type` CHECK，加入 `local_culture_jh`、`general_books_jh`（fresh DB 策略）
- 新增 `migrations/005_expand_project_type_checks.sql`：使用 `PRAGMA foreign_keys=OFF` + table rebuild 模式升級既有 DB，分別重建 `procurement_projects` 與 `export_templates`
- 更新 `app/services/export_service.py` 錯誤訊息，移除 `config.yaml` 引導，改指向「範本管理」
- 新增 `tests/test_project_type_checks.py`：涵蓋 fresh DB（案例 A）與模擬舊 DB 升級（案例 B），含四種合法類型寫入、無效類型拒絕、既有資料保留

## 驗證到哪

- `git diff --check`：通過
- `.venv\Scripts\python.exe -m pytest -q tests/test_project_type_checks.py`：4 passed
- `.venv\Scripts\python.exe -m pytest -q`：74 passed，3 warnings（既有）

## 未解決的問題

無

## 下次繼續

已完成
