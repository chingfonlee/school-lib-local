---
date: 2026-06-23
ended_at: 2026-06-23T19:46:32
task: task-project-card-actions
agent: claude
---

## 做了什麼

任務完成。

### 前端（app/static/projects.html）

- 新增 `enterProject(id, name)` helper：呼叫 `setProject` 後導向 `/import.html`
- 整個卡片加上 `onclick="selectCard(id, name)"`，點擊主體即選擇專案
- 三個操作按鈕（選擇、設定、刪除）全部加上 `event` 參數並在函式內呼叫 `event.stopPropagation()`
- 移除 `confirmDelete` 的 early return（原本阻擋刪除 current project），改為在 modal 內顯示 `#delete-current-warning` 橙黃色警告
- `selectProject` 導向頁面從 `/index.html` 改為 `/import.html`
- 卡片 template 加上 `.current` class（當 `cur == p.id` 時）

### CSS（app/static/css/style.css）

- `.project-card` 加入 `position: relative`、`border: 1px solid rgba(0,0,0,0.04)`、`transition` 含 `transform`
- 新增 `::before` 偽元素實作左側藍色導引線（`top: 16px; bottom: 16px; width: 4px; border-radius: 0 4px 4px 0`）
- hover 狀態：`box-shadow: 0 8px 24px rgba(0,0,0,0.14)`、`transform: translateY(-1px)`、`border-color: rgba(0,113,227,0.2)`、`::before background: #0071e3`
- `.project-card.current`：永久顯示藍色 `::before` + 淡藍 border

### Spec / Plan

- 新增 `docs/tasks/task-project-card-actions/task-project-card-actions-spec.md`（含需求一到四）
- 新增 `docs/tasks/task-project-card-actions/task-project-card-actions-plan.md`（Steps 1–6，含 CSS 視覺強化）
- 中途新增需求四（卡片視覺回饋強化），spec/plan 同步更新

## 驗證到哪

- pytest：58 passed（實作完成後執行，全部通過）
- 手動驗收：由使用者確認完成

## 未解決的問題

無

## 下次繼續

已完成
