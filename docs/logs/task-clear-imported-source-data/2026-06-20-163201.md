---
date: 2026-06-20
ended_at: 2026-06-20T16:32:01
task: task-clear-imported-source-data
agent: claude
---

## 做了什麼

任務完成。新增正式「清除來源資料」功能：
- `app/services/import_service.py`：新增 `_clear_library_holdings(conn)` helper、`clear_library_holdings(user_id)` 與 `clear_vendor_books(project_id, user_id)` public functions。
- `app/routers/imports.py`：新增 `DELETE /api/imports/holdings` 與 `DELETE /api/imports/vendor-books?project_id=N`，均需身份驗證。
- `app/static/import.html`：館藏與書商書單區塊各加入危險操作按鈕與雙重確認流程（confirm + prompt 輸入「清除」）。

## 驗證到哪

- `python -m compileall app` pass。
- 臨時 SQLite DB service-level 測試：清館藏正確刪 holdings/matches/batches；清書商書單後 `selection_items` 保留原有筆數；project_id 不存在回 ValueError。

## 未解決的問題

無

## 下次繼續

已完成
