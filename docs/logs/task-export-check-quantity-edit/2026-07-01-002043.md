---
date: 2026-07-01
ended_at: 2026-07-01T00:20:43
task: task-export-check-quantity-edit
agent: claude
---

## 做了什麼

任務完成。

新增「匯出前檢查頁可修改選書數量」功能：

1. `app/services/selection_service.py`：新增 `update_selection_quantity(selection_id, quantity, user_id)`，只更新 `selected_quantity` 與 `updated_at`，不動 `notes`/`user_overrides`。
2. `app/routers/selections.py`：新增 `QuantityPatch(Field ge=1)` model 與 `PATCH /api/selections/{id}/quantity` endpoint；`ValueError` → 404、驗證失敗 → 422。
3. `app/static/export-check.html`：表格新增「數量」欄（thead + 每列 number input min=1）、新增 `patchQuantity()` 函式（`input.valueAsNumber` + `Number.isInteger()` 驗證）、colspan 6→7 同步調整。
4. `tests/test_selection_quantity.py`：7 個測試案例（更新、不清 notes、不清 user_overrides、float 422、zero 422、negative 422、404）。

修正事項（code review 補丁）：
- 前端由 `parseInt(input.value, 10)` 改為 `input.valueAsNumber`，正確拒絕 1.5。
- 新增 `test_patch_quantity_float_returns_422` 鎖定 spec。

## 驗證到哪

- `tests/ -q`：81/81 passed。
- `curl PATCH qty=3` → 200，DB 確認 selected_quantity=3。
- `curl PATCH qty=0` → 422，`qty=-1` → 422，`qty=1.5` → 422，不存在 sel_id → 404。
- 預算金額驗證：qty=1 → NT$17,156；qty=3 → NT$17,636；差額 NT$480 = 240×2，正確。
- Playwright 截圖：「數量」欄可見、input 值正確顯示、預算摘要連動。
- Excel 匯出未手動執行（`selected_quantity` 已是既有匯出欄位，未改匯出邏輯）。

## 未解決的問題

無

## 下次繼續

已完成
